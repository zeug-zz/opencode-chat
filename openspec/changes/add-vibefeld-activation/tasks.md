## 1. AF Discovery and Pinned Preflight

- [x] 1.1 Implement host-owned AF discovery from fixed candidate locations plus `PATH`, resolving only the host-owned executable and ignoring any executable, path, environment, or argv from a model, prompt, plugin, MCP, webview, or user message; verify focused discovery tests cover found, absent, ambiguous, non-executable, non-owned, and model-supplied inputs and that a dormant `unavailable` result is returned without side effects for every failure case.
- [x] 1.2 Add the compatibility preflight that validates the live `af version --json` result against the format-pinned, version-tolerant contract (a version matching `^0\.1\.\d{1,3}$`, `format` exactly `"1.1"`, all required fields present, bounded at ≤256 chars, and safe; platform and architecture host-derived) and classifies the runtime as `available`, `incompatible`, or `unavailable`; verify a version outside 0.1.x or a non-`1.1` format reports `incompatible`, missing, malformed, oversized, unsafe, ambiguous, or non-owned evidence stays dormant as `unavailable`, unsupported platforms report dormant without a process attempt, recorded `version`/`commit`/`build_date`/`go_version`/`policy` never gate support by exact equality, and no executable path or raw preflight output leaves host-private state.
- [x] 1.3 Add negative coverage proving discovery and preflight never spawn on unsupported platforms, never allocate a proof workspace before policy readiness, never write AF/OpenCode/nono configuration, and leave ordinary Chat and Write unchanged; verify the focused security-negative assertions for discovery fail closed.

## 2. Production Policy Adapter

- [x] 2.1 Implement the production macOS/Linux `AfExecutionPolicyAdapter` over a validated AF nono profile selection: `readiness.state === "ready"` only from that selection, `shell: false`, inherited descendant confinement, deny-unapproved child execution, the denied domains `repository`, `home-directory`, `opencode-state`, and `sibling-review-roots`, bounded stdin/stdout/stderr, and terminate-and-reap; verify injected-seam adapter tests cover ready, unavailable, ambiguous, invalid-selection, and unsupported-platform inputs without requiring a real nono profile.
- [x] 2.2 Enforce selection-only persistence and fail-closed fallback: persist only a validated profile selection name, never create/edit/promote/broaden a nono profile, never reuse, reconfigure, or query the Chat sandbox policy, and never retry unsandboxed; verify injected-seam tests assert no profile write, no Chat sandbox reuse, no unsandboxed retry, and bounded cleanup-failure classification.
- [x] 2.3 Add a fail-closed test that a denied-domain or escape attempt, a cleanup failure, and a policy teardown failure produce audit-failed/unavailable results and refuse further runtime operations; verify the adapter tests assert no weaker policy, no broader grant, and no process surviving reaping.

## 3. Live Output Parsers and Captures

- [x] 3.1 Add sanitized versioned captures of the real CLI shapes — flat `af version --json` fields, the six `af schema --format json` arrays, `af init` prose with the workspace path discarded, and `af status --format json` statistics/jobs/nodes/challenges — and implement the live parsers against them; verify live-parser tests produce normalized host-private facts (bounded schema section counts, `{ initialized: true }` for a success init exit, and bounded numeric status aggregates), derive platform and architecture from the host, and never expose raw output, paths, ledger data, node or challenge content, or a structural claim.
- [x] 3.2 Keep the fixture-schema parser (including the synthetic `af-runtime-fixture-1` envelope) as the explicit test double with mode-based parser selection; verify both parsers remain separate modules, a missing or mismatched mode fails closed as unavailable, fixture-shaped output (schema markers, placeholders, fixture workspace identity) is rejected in live mode, the fixture envelope is never production evidence, and default tests use fixtures or injected seams only.
- [x] 3.3 Add the gated live-capture harness (explicit opt-in, project-relative sanitized output, bounded fields) that produces new sanitized captures of the real CLI shapes; verify the harness is skipped by default, its sanitizer rejects unsafe values and strips workspace paths before writing, and no default test invokes a shell, process-spawn API, or live AF command.
- [x] 3.4 Migrate the pin to the format-pinned, version-tolerant contract across `af-compatibility.ts`, `af-output-schema.ts`, `af-runtime-contract.ts`, and their focused tests: accept any bounded, safe `^0\.1\.\d{1,3}$` version reporting `format` exactly `"1.1"`, record `version`/`commit`/`format`/`policy` as host-private compatibility metadata without exact commit equality, derive platform and architecture from `process.platform`/`process.arch`, and retire the `af-runtime-fixture-1` envelope to test-double status; verify focused `af-compatibility`, `af-output-schema`, and `af-runtime-contract` tests cover 0.1.x acceptance, a version outside 0.1.x, a non-`1.1` format, missing/malformed/oversized/unsafe fields, unsupported platform and architecture, and a real-shape capture with an unrelated commit that still resolves `available`.

## 4. Activation Composition

- [x] 4.1 Construct the runtime bridge and proof store in `extension.ts` beneath `context.globalStorageUri` with session-scoped review roots, consuming mode-selected live facts (never fixture-only production evidence) and the `{ version, commit, format, policy }` compatibility metadata; verify construction tests assert the root resolves under global storage and never under the repository, home directory, OpenCode state, or a sibling review root.
- [x] 4.2 Preflight the bridge at most once per extension activation and inject `ClaimProjectionReasoningReviewController` when the runtime is ready, otherwise `UnavailableReasoningReviewController`, with nonfatal failure handling; verify dynamic-selection tests cover ready, unavailable, incompatible, policy-unavailable, and preflight-failure inputs, assert no production path uses the fixture-only parser or fixture-shaped output as live evidence, and assert no per-message discovery or preflight occurs.
- [x] 4.3 Publish the bounded runtime status over the existing `reasoningRuntime` protocol message and keep ordinary Chat, Write, Scout, worker, MCP, sandbox, and TUI behavior unchanged when dormant; verify extension-host tests cover status publication, dormant no-side-effect behavior, and unchanged manual review fallback.
- [x] 4.4 Wire production runtime resolution into the activation composition: host-owned AF executable discovery with real filesystem/PATH seams, nono executable resolution and AF profile validation through the bounded `nono profile show` preflight, and a documented AF profile selection (fixed documented default name with a host-private setting override) whose readiness derives only from that validation; pass the resolver and policy into `createVibefeldActivation` so a compatible macOS/Linux host with a promoted AF profile can compose and preflight, while absent, ambiguous, non-owned, missing-nono, invalid-selection, and stale-selection cases stay dormant without side effects; verify resolution tests cover found/absent/ambiguous/non-owned executables, missing/invalid/stale profile selections, missing nono executables, no profile writes, no Chat sandbox reuse, and no process launch without a composed policy.

## 5. Settings and Localization

- [x] 5.1 Add the reasoning-review preference at the Global target with a per-workspace opt-out and effective state `userEnabled && runtime available && policy ready`; verify settings tests cover the default, workspace opt-out, Global preference inheritance, and absent/incompatible runtime states without writing unrelated configuration.
- [x] 5.2 Add the availability-gated `ToolConfigPanel` control and synchronized keys in every webview locale dictionary; verify component and scenario tests render the control only when the runtime is available and the policy is ready, never render a non-functional control, and resolve every new key under every supported locale.

## 6. Automatic-Routing Enablement

- [x] 6.1 Inject the qualified evaluation and enable flag into the existing automatic-routing integration; verify selection tests assert no selection unless `validateAutomaticRoutingEvaluation` reports qualified, the runtime is available, the enable flag is set, and the existing policy signals pass, with thresholds, exclusions, duplicate suppression, and manual precedence unchanged.
- [x] 6.2 Verify fail-closed routing and bounded rationale behavior end to end: unqualified, stale, malformed, below-minimum, or over-target evaluations, unavailable runtime, disabled flag, and policy-signal failure produce no selection and no controller invocation; bounded rationale still renders for valid automatic selections; and no response is withheld, rewritten, gated, or released.

## 7. Hybrid Qualification Pipeline

- [x] 7.1 Add the versioned 30–50 case adjudicated seed corpus as a repository artifact and run it through the existing measurement and qualification path; verify the corpus is checked in, contains adjudicated bounded fields with no user content or paths, and the pipeline produces aggregate metrics the existing validator processes.
- [x] 7.2 Add the host-private aggregate live recorder: latency recorded automatically, `challenged` derived from review status, `correct`/`falseChallenge` collected only through bounded local review-card feedback, accumulating to the existing 100-case minimum; verify recorder tests reject prompts, source packets, review text, response text, and paths, assert no Hindsight or memory-provider runtime call, and confirm qualification is not reported before the case minimum.
- [x] 7.3 Add the escape hatches and host-private retention: workspace opt-out and disable stop live capture and automatic routing, and aggregates stay local to the host; verify disable/opt-out tests record no further aggregates and assert no automatic ingestion of review content into memory.

## 8. Security-Negative Amendment

- [x] 8.1 Amend the security-negative requirements only through the explicit MODIFIED deltas in this change and update the focused suites to permit the constrained wiring while retaining prohibitions on plugins, MCP, custom tools, agent overlays, `IAgent`/protocol AF fields, arbitrary argv, and unsandboxed retries; verify the amended suites still fail on any broader authority, including a deliberately widened fixture.
- [x] 8.2 Add focused negative coverage of the constrained wiring itself: no arbitrary argv, executable, workspace, or environment input; no model-visible route; no Chat sandbox reuse; no nono profile write; no response-gate, adversarial, or Hindsight activation; verify the negative tests pin each prohibition and the extension, agent, launch-configuration, and protocol sources remain free of model-visible AF surfaces.

## 9. Verification

- [x] 9.1 Run the focused discovery, policy adapter, parser, composition, settings, routing, qualification, and security-negative suites and verify dormant behavior, availability gating, fail-closed selection, aggregate-only qualification, and localized rendering.
- [x] 9.2 Run `pnpm run check`, `pnpm run build`, and `pnpm run test:all`; verify ordinary Chat and Write behavior and the existing security boundaries remain unchanged.
- [x] 9.3 Run `openspec validate add-vibefeld-activation --strict` and `git diff --check`; inspect the final diff for scope creep, project-relative scratch paths only, untouched prior changes, no generated artifacts, and no commit, push, or archive operations.

## 10. Direct Execution Rework (KISS)

- [x] 10.1 Replace the dedicated-policy contract with the direct-execution contract across `af-execution-boundary.ts` and `af-process-executor.ts`: readiness `{ state: "ready", execution: "direct" }`, descriptor `{ executable, argv, cwd }`, no grants, no denied domains, no audit fields; verify focused executor/boundary tests cover direct readiness, fixed argv, `shell: false`, bounded stdio, terminate-and-reap, and unavailable platforms without any nono or sandbox call.
- [x] 10.2 Add `af-direct-policy.ts` implementing the direct-execution adapter with `spawn(executable, argv, { shell: false, detached: true, stdio, env: allowlist })`, bounded output, and terminate-and-reap; delete `af-nono-policy.ts` and its tests; verify focused tests prove no nono invocation, no profile flags, no grants, and no nested sandbox.
- [x] 10.3 Simplify runtime resolution and activation wiring: keep AF discovery and pinned compatibility only, delete `af-profile-selection.ts`, the `vibefeld.nonoProfile` override, the `nono profile show` preflight, and `docs/vibefeld-nono-profile.md`; `extension.ts` constructs the composition with direct execution; verify dormancy on absent/ambiguous/non-owned AF and supported-platform readiness with no nono calls.
- [x] 10.4 Update the security-negative suites and plan/addendum wording: pin that the AF path never invokes nono (`nono` binary, `wrap`, `profile`, `--read`/`--allow`), never selects or writes profiles, and never nests sandboxes; retain all model-visible-route, arbitrary-input, plugin/MCP/tool/agent prohibitions; verify the widened-fixture negative still fails closed.
- [x] 10.5 Re-run `pnpm run check`, `pnpm run build`, `pnpm run test:all`, `openspec validate add-vibefeld-activation --strict`, and `git diff --check`; verify ordinary Chat/Write and prior security boundaries are unchanged and no generated artifacts or scratch files remain.

## 11. Review Availability and Toggle Correction

- [x] 11.1 Gate controller selection and runtime publication on the claim capability: in `extension.ts`, inject `ClaimProjectionReasoningReviewController` only when the bridge preflight is ready and `createCurrentVibefeldClaimProjectionSeam(...).getCapability().supported` is true; otherwise inject `UnavailableReasoningReviewController` and publish the bounded `{ state: "unavailable", reason: "claim-capability-unavailable" }` status. Keep seam construction side-effect-free, keep the single activation preflight, and verify automatic routing stays inactive, the effective preference stays disabled, and no qualification evidence is recorded in that state; update the existing extension tests that assert a ready bridge injects the claim controller.
- [x] 11.2 Hide the per-message review affordance when review is unavailable: `MessageItem` SHALL render the review action only for a completed assistant message while `reasoningReviewRuntime.state === "available"`; verify completed assistant messages with a null, `unavailable`, `checking`, or `incompatible` runtime render no action and send no review request, while an existing summary card render stays bounded.
- [x] 11.3 Add the open/close toggle for a completed review: with a summary and an available runtime, the affordance SHALL collapse the card (`message.hideReview`) and expand it (`message.showReview`) without sending `requestReasoningReview`; it SHALL expose `aria-expanded` and remain the cancel action while a review is in flight; add both keys to every webview locale dictionary and `locales.test.ts`; extend `MessageItem` tests for hide, show, no-request-on-toggle, and cancel.
- [x] 11.4 Extend the security-negative and regression suites: pin that no claim controller is injected without an explicitly supported claim operation, that no fixture seam reaches production, that availability is never published without review capability, and that routing and qualification stay inactive while unavailable; keep every retained prohibition failing closed; run the focused security, extension, and webview suites.
- [x] 11.5 Run `pnpm run check`, `pnpm run build`, `pnpm run test:all`, `openspec validate add-vibefeld-activation --strict`, and `git diff --check`; verify ordinary Chat/Write and prior security boundaries are unchanged and no generated artifacts or scratch files remain.
