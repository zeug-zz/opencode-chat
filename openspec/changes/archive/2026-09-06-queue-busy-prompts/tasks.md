## 1. Host queue contract and lifecycle

- [x] 1.1 Add the typed `queuedPrompts` host-to-webview message in `packages/core/src/protocol.ts` and implement the in-memory per-session normal-send coordinator in `packages/platforms/vscode/src/chat-view-provider.ts`: synchronous active admission, complete payload snapshotting, FIFO storage, matching-idle one-item draining, duplicate/foreign status protection, abort preservation, deletion cleanup, initialization/active-session count publication, and failed-dispatch retention through the existing error path. Leave UI-to-host send payloads, `IAgent`, SDK calls, edit-and-resend, and unrelated session behavior unchanged. Verify the touched TypeScript with the focused host test command and `npm exec biome check packages/core/src/protocol.ts packages/platforms/vscode/src/chat-view-provider.ts`.
- [x] 1.2 Add focused host regression tests in `packages/platforms/vscode/src/__tests__/chat-view-provider.test.ts` for idle admission, rapid sends before busy, exact FIFO ordering and one-item-per-idle behavior, full payload/mode preservation, abort ordering, foreign-session isolation, deletion, initialization count restoration, and failed-dispatch retention. Verify with `npm run test:ext -- src/__tests__/chat-view-provider.test.ts` from `packages/platforms/vscode` and `git diff --check`.

## 2. Shared webview queue feedback

- [x] 2.1 Update `packages/platforms/vscode/webview/App.tsx` to consume session-scoped `queuedPrompts` messages, clear counts on active-session changes, and pass the active count to `InputArea`; update `InputArea.tsx` to submit Enter while busy without weakening IME/popup behavior, keep the Send/Stop switch, and render an accessible compact queued-count indicator; add only the required CSS-module styling in `InputArea.module.css` and localized queue label/accessibility strings in every supported `webview/locales/*.ts`. Preserve attachments, model/effort/agent/guidance capture, history, and all existing mode behavior. Verify the focused webview scenarios and `npm exec biome check` on the touched files.
- [x] 2.2 Update `packages/platforms/vscode/webview/__tests__/scenarios/03-messaging.test.tsx` and `packages/platforms/vscode/webview/__tests__/scenarios/13-keyboard-ime.test.tsx` to cover the localized indicator, session-scoped count, busy Enter admission, Stop with pending prompts, idle-driven dispatch, and preserved IME suppression while retaining existing send/stop assertions. Verify with `npm test -- webview/__tests__/scenarios/03-messaging.test.tsx webview/__tests__/scenarios/13-keyboard-ime.test.tsx` from `packages/platforms/vscode` and `git diff --check`.

## 3. Cross-layer completion verification

- [x] 3.1 Audit the complete queue path against `openspec/changes/queue-busy-prompts/specs/prompt-queue/spec.md` and `design.md`, make only focused corrections in the already-scoped queue source/test files if a clear contract gap remains, and verify no SDK upgrade, persistence/config write, permission/sandbox/MCP change, or unrelated task was introduced. Run the focused host and webview queue tests, `npm exec biome check` on all changed implementation/test files, `openspec validate "queue-busy-prompts" --strict`, and `git diff --check`.
- [x] 3.2 Run the final affected-package verification: `npm run test:all`, `npm run check`, and `npm run build` from the repository root, plus strict OpenSpec validation and final worktree/diff scope review. Confirm all queue scenarios pass, no generated scratch artifacts exist, and the change is archive-ready without archiving or committing it.
