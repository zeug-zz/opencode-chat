## 1. Companion agent overlay and regression coverage

- [x] 1.1 Update the extension-owned `CHAT_AGENT_OVERLAY` so Scout's task permission is an ordered exact allowlist for `chat-research-worker`, inject that worker as a `subagent`, and give it only the explicit read/discovery/web and `firecrawl_*`, `context-mode_*`, and `paper-search_*` permission prefixes with catch-all denial; preserve Build/Write denials, MCP preference gating, no config writes, and independent TUI behavior. Add focused tests for the effective unsandboxed and sandboxed overlays, exact and near-miss task targets, worker tool denials/allowances, disabled versus enabled research MCP overlay state, and unchanged Build denials; add only the focused existing webview agent-discovery/child-session assertion needed to prove the worker remains subagent-only. **Verify:** `npm test -- src/__tests__/opencode-agent.test.ts` from `packages/agents/opencode`, the focused initialization/child-session webview tests from `packages/platforms/vscode`, and `npm exec biome check src/opencode-agent.ts src/__tests__/opencode-agent.test.ts` from `packages/agents/opencode`.

## 2. Prompt boundary guidance

- [x] 2.1 Update `packages/platforms/vscode/CHAT_SYSTEM.md` with bounded Scout-supervisor guidance for independent research delegation, evidence/provenance and citation requirements, untrusted child/MCP/web output, verification and synthesis, and explicit refusal of coding/shell/file/package delegation; clarify that report authoring goes to Write and serious coding or shell work goes to the TUI. Update `WRITE_SYSTEM.md` to state explicitly that Write never uses task or subagent workflows. **Verify:** inspect the prompt diff for the required boundary language and run `git diff --check` without changing implementation or OpenSpec artifacts.

## 3. Security and capability documentation

- [x] 3.1 Update `SECURITY.md`, `AGENTS.md`, and `plans/server-hardening.md` so the documented Scout capability matrix and current baseline describe exact restricted delegation to the injected read-only worker, deny arbitrary or recursive delegation, keep Write non-delegating, identify the reviewed research MCP prefixes and downstream MCP trust boundary, and retain the independent TUI and sandbox protections. Preserve historical records and do not alter user configuration guidance beyond this capability correction. **Verify:** run a focused content audit for the exact worker name, approved prefixes, wildcard/default-deny language, Write task denial, and TUI handoff; run `git diff --check`.
