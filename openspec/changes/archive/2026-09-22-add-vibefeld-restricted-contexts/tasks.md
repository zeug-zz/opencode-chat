## 1. Hidden Restricted Agent Overlay

- [x] 1.1 Extend `OpenCodeLaunchConfiguration` with one restricted-review agent entry and merge it into the in-memory overlay built by `buildChatOverlay`: a deny map derived from the authoritative tool and permission inventory (every concrete built-in authority — `read`, `glob`, `grep`, `edit`, `write`, `patch`, `bash`, `task`, `webfetch`, `websearch`, `skill`, `todo`/`todowrite`, `question`, `lsp`, `list`, `external_directory`, `doom_loop` — plus any dynamically present plugin/MCP tool names, plus the `"*"` wildcard, rejecting unknown entries), a fixed host-owned `prompt` instruction, the host-resolved child model, bounded `maxSteps`, and hidden mode semantics; verify focused tests assert the overlay is in-memory only, is never written to a global or workspace config file, profile, plugin, or MCP entry, rejects permissive, partial, unknown-authority, or unknown-field overlays before any context exists, and keeps the restricted agent out of user-visible agent surfaces.

## 2. Agent-Package Restricted Provider

- [x] 2.1 Add the narrow restricted-context provider module in `packages/agents/opencode/` (session create, bounded stage prompt with the restricted agent and deny map, bounded text retrieval within the stage deadline, cancel, delete, plus host-pinned model resolution) and export it from the package index without touching `IAgent` or `packages/core`; verify focused tests with an injected SDK-client fake cover session creation, prompt bodies containing only the bounded packet, host-only model resolution (never packet- or request-derived), bounded retrieval and discard of stage text, stage cancellation, session deletion, and no shell/exec/file authority use.
- [x] 2.2 Mint bounded letter-leading provenance identities and handles (`prover-…`/`verifier-…`, `≤64`, `^[A-Za-z][A-Za-z0-9_-]*$`) with the fixed context numbers, never derived from packet content, and implement the configuration-verified, generation-bound readiness preflight: host-composed overlay for the current generation plus read-back showing the restricted agent with every enumerated tool denied and the expected model and instructions; verify focused tests assert minted shapes satisfy the existing validators, distinct-context validation passes, the check is read-only (no session, model call, process spawn, or config write), and absent, drifted, permissive, malformed, or stale-generation states keep the provider dormant and invalidate a prior ready result on reconnect or overlay change.
- [x] 2.3 Enforce a provider-local stage deadline (≤30 seconds) around bounded stage text retrieval, make cancellation awaitable, and check a publication generation token before any result is published; verify focused tests cover deadline elapse mapping, awaited cancellation ordering against a new review, late/superseded result drop, no automatic retry, and raw child text never being retained beyond validation.

## 3. Vscode Restricted Adapter

- [x] 3.1 Wrap the provider into `RestrictedReviewAdapter` in `packages/platforms/vscode/src/vibefeld/` with the exact fixed attestation, the bounded failure mapping for create/run/cancel results, and controller wiring that sets the orchestrator's explicit total timeout (default 60 s) at construction, replacing the unusable 5 s default; verify focused tests assert the existing validators accept only the exact profile, the explicit total is applied where the controller constructs the orchestrator, a widened or permissive adapter fails closed before any context creation, and no fixture or test adapter is reachable from production wiring.
- [x] 3.2 Normalize stage-derived reason text before projection with host-applied redaction of path-, URL-, and secret-like patterns plus hard bounds, so raw child text never reaches the published `ReasoningReviewSummary`; verify focused tests assert redaction of representative path/URL/secret-like samples, hard length bounds, and that no unredacted child text is projected through the mapper or published.

## 4. Selection Wiring

- [x] 4.1 Select the claim-projection controller when the claim capability is available, select the adversarial controller only when claim projection is unavailable and the provider readiness preflight is ready, and keep the bounded unavailable controller otherwise; keep selection side-effect free with no context, session, or model call before a manual request, invalidate readiness on reconnect/overlay change, and await child-session disposal on deactivation; verify focused extension tests cover the precedence both ways, the fallback, the dormant-by-construction current-host behavior, exactly-one-preflight and no re-probe on status reads, generation invalidation, the awaited deactivation cleanup, and no webview or protocol change.
- [x] 4.2 Pin adversarial review to the manual trigger: automatic routing SHALL not invoke the adversarial controller, the controller SHALL reject any non-manual trigger, and readiness SHALL be revalidated for the current generation before a review starts; verify focused tests assert an automatic trigger creates no child context or model request, a rejected non-manual trigger maps to no-op/unavailable, a stale-generation readiness fails the review safely, and manual requests remain the only working entry point.

## 5. Hidden And Unretained Lifecycle

- [x] 5.1 Add the host-side hidden-session registry: register every child session id at creation, filter registered ids from the extension session-list mapping, agent lists, and webview event publication, and buffer events that arrive before registration resolves so hiddenness never depends on title matching; keep fixed marker titles containing only a random token for startup scavenging, and enforce at most two live child sessions and one in-flight review per session/message; verify focused tests cover registry filtering of session lists, agent lists, and events (including the pre-registration event race), marker title content exclusion, the concurrency bounds, and no packet, proposal, verdict, or trace persistence.
- [x] 5.2 Abort and delete child sessions unconditionally in a `finally` on success, failure, timeout, or cancellation; await disposal during extension deactivation; and scavenge leftover marked sessions at startup; verify focused tests cover delete-on-every-outcome including clean success, awaited deactivation ordering, startup scavenging of a leftover marked session, and that no global or workspace configuration file is written anywhere in the lifecycle.

## 6. Security Negatives

- [x] 6.1 Amend the focused security negatives only through the explicit spec deltas: add pins that the restricted overlay exists only in the in-memory configuration content, the provider is internally constructed and unreachable from model-visible/task/plugin/MCP/tool routes, no configuration file, profile, or workspace write occurs, the restricted agent name is absent from user-visible agent lists, readiness is generation-bound and never published as enforced isolation, host-configured plugins/MCP remain outside the denial boundary, automatic routing cannot invoke the adversarial controller, and a deliberately widened provider or attestation fails closed; retain every existing prohibition.

## 7. Gated Live Proof And Verification

- [x] 7.1 Add the gated live proof suite reusing the repository's existing live-test opt-in with a disposable host-owned configuration: assert the effective config denies every enumerated tool for the restricted agent, including a dynamically added tool, and the hidden agent is directly invokable for both roles; run a sentinel-file read probe and assert the response contains no tool invocation and no sentinel content; assert distinct minted prover/verifier provenance; assert cancellation aborts and deletes the sessions and that deletion removes child sessions after both successful and failed reviews; assert marked sessions stay hidden from the extension session list; and assert no global or workspace configuration file is written; verify the default run skips with no server launch, model call, or network request.
- [x] 7.2 Run the focused overlay, provider, adapter, selection, lifecycle, security-negative, and gated-proof suites; verify the default suite requires no server, model provider, network, profile, or configuration write and every dormant and failure path stays bounded.
- [x] 7.3 Run `pnpm run check`, `pnpm run build`, and `pnpm run test:all`; verify ordinary Chat, Write, Scout, worker, MCP, sandbox, and TUI behavior is unchanged and no core, protocol, or webview file is modified. Run `openspec validate add-vibefeld-restricted-contexts --strict` and `git diff --check`; inspect the final diff for scope creep, project-relative scratch paths only, untouched predecessor changes, no generated artifacts, and no commit, push, or archive operation.
