## 1. Observed Claim And Refine Capture Shapes

- [x] 1.1 Extend `af-capture-harness.ts` and its gated integration test with the observed `claim` and `refine` shapes: fixed argv with `<node-id>`, `<statement>`, `<owner>`, and `<workspace>` placeholders, JSON sanitization that redacts `context` and the existing content fields for the content-bearing operations, manifest entries for both shapes, the six-shape sequence (`version`, `schema`, `init`, `claim`, `refine`, `status`) in one disposable project-relative workspace with `init` before `claim` and `claim` before `refine`, and staged-set retention only behind an explicit keep flag; verify focused harness tests cover the new shapes, content redaction, unsafe/oversized rejection, the file-name pattern, and the unchanged default-skip behavior, and that the gated suite performs no resolution, allocation, or spawn before its gate is satisfied.
- [x] 1.2 Produce the refreshed sanitized `live/af-0.1.11` capture set from one gated run with the real AF 0.1.11 executable and store `claim.json`, `refine.json`, the refreshed `status.json`, and the updated `manifest.json` (with `version.json`, `schema.json`, and `init.txt` regenerated consistently from the same run); verify every stored shape parses as live output, no statement, context, host path, or executable path is stored, only the six documented commands plus placeholders appear in the manifest, and the staging root is removed.

## 2. Fixed Operation Union Extension

- [x] 2.1 Add typed `claim` (`node identifier`, `role` in `prover`/`verifier`, host-fixed `owner`) and `refine` (`parent identifier`, bounded statement list, host-fixed `owner`) operations to `af-command-schema.ts` with the exact observed argv (`claim <node-id> --owner <owner> --role <role> --dir <workspace> --format json`; `refine <parent-id> <statement>... --owner <owner> --dir <workspace> --format json`), bounded inputs within the execution-boundary argument limits, and no caller-supplied executable, workspace, owner, or raw argv; verify focused schema tests cover valid encoding, unknown-operation rejection, absent/unsafe/oversized inputs, statement-count bounds, and shell-token rejection, and that the exported operation list covers exactly the six approved operations.

## 3. Live Parsers And Executor Support

- [x] 3.1 Add strict live parsers for the captured `claim` and `refine` shapes that retain only bounded host-private facts (claim: node id, allowlisted role, `claimed` status; refine: parent id, child ids in order, child count), discard `context`, statements, timestamps, and all other fields, and reject fixture markers, malformed, oversized, unsafe, or execution-failure results; verify focused parser tests parse the stored capture shapes and reject malformed, oversized, unsafe, fixture-shaped, non-zero, cancelled, timed-out, and signaled inputs.
- [x] 3.2 Extend `AfOutputParserSet`, `af-parser-mode.ts`, the fixture-mode test double, and `af-process-executor.ts` (per-operation parser selection and timeouts) to cover the extended union, with explicitly closed claim/refine fixture parsers and unchanged fixture/live separation; verify per-operation timeout coverage, fixture-mode claim/refine fail closed without inventing fixture facts, and production selection still returns only the live parser set.

## 4. Bridge Capability And Review-Root Rotation

- [x] 4.1 Extend `VibefeldRuntimeBridge` with the contracted `claim` and `refine` operations: typed union and facts, `initialized` gating so claim/refine/status stay unavailable before a successful `init`, bounded failure mapping, and an explicit side-effect-free capability report that is exactly `{ supported: true, operation: "claim_projection" }`; verify focused bridge tests cover the extended union, initialized gating, capability reporting without preflight/spawn/allocation, and `structuralStatus: null` on every result.
- [x] 4.2 Add review-root rotation as a bounded bridge lifecycle step: reuse the preflight-allocated root for the first projection, clean up and allocate a fresh contained root once a projection has initialized the current one, fail closed as audit-failed on cleanup or allocation failure, and refuse further operations after audit failure; verify focused tests cover reuse, rotation, containment/cleanup failure, and no retry with weaker containment.

## 5. Bridge-Backed Projection Seam

- [x] 5.1 Implement the production projection over the real bridge in `current-vibefeld-claim-projection.ts` (or one adjacent private module that the current factory composes): capability read from the bridge report with an exact-shape check, host-fixed prover identity, the bounded sequence (rotate/reuse root, `init` conclusion, `claim` root, chunked `refine` of remaining statements, `status` read-back), refusal of out-of-bounds statements or counts without truncation, cancellation propagation through every operation, and the calibrated derivation (complete recording -> `structurally_checked`; incomplete/unexpected -> `unresolved`; bounded failure -> `unavailable`/`audit_failed`); verify focused tests cover clean projection, chunking, expected-count mismatch, bounds refusal, cancellation mid-sequence, each failure class, and that capability reads and seam construction never preflight, spawn, or allocate.
- [x] 5.2 Replace the hard-coded unsupported claim boundary: the current factory SHALL report the bridge capability and SHALL compose the unsupported seam only for a boundary that does not report the exact contracted capability; verify dormant, incompatible, unready, and capability-less boundaries still project to `{ status: "unavailable", reason: "unsupported" }` without calling `preflight`, `run`, or `teardown`, and that the fixture test seam remains unreachable from production modules.

## 6. Selection, Security Negatives, And Live Integration

- [x] 6.1 Verify the activation gate selects `ClaimProjectionReasoningReviewController` and publishes `available` only when the composed bridge reports the contracted capability; add or adjust focused extension tests for the claim-capable path (select projection controller, publish ready status, still exactly one activation preflight) and keep the bounded `claim-capability-unavailable` fallback for every other preflight state.
- [x] 6.2 Amend the focused security negatives only through the explicit spec deltas: replace the hard-coded-unsupported pins with assertions that permit only the constrained bridge-backed wiring (bridge capability report read before controller construction, fail-closed fallback preserved) while retaining every existing prohibition (no fixture seam in production, no model-visible route, no arbitrary argv/executable/workspace/owner, no Chat sandbox or profile use, no broader AF authority); verify a deliberately widened bridge boundary still fails closed.
- [x] 6.3 Extend the gated live integration suite to run the contracted claim sequence (`init`, `claim`, `refine`, `status`) in the disposable review root and assert bounded facts, `shell: false`, close-to-limit argv, descendant cleanup, and no raw path or payload leakage; verify the suite skips by default and launches nothing before its gate is satisfied.

## 7. Verification

- [x] 7.1 Run the focused capture, schema, parser, executor, bridge, seam, extension, and security-negative suites; verify the default suite requires no AF binary, profile, network, or global configuration and every dormant and failure path stays bounded.
- [x] 7.2 Run `pnpm run check`, `pnpm run build`, and `pnpm run test:all`; verify ordinary Chat, Write, Scout, worker, MCP, sandbox, and TUI behavior is unchanged and no core, protocol, or webview file is modified.
- [x] 7.3 Run `openspec validate add-vibefeld-claim-bridge --strict` and `git diff --check`; inspect the final diff for scope creep, project-relative scratch paths only, untouched predecessor changes, no generated artifacts or leftover staging roots, and no commit, push, or archive operation.
